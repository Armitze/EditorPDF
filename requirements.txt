fastapi>=0.115
uvicorn>=0.30
pywebview>=5.0
PyMuPDF>=1.24
pyinstaller>=6.0 ; python_version >= "3.10"
Pillow>=10.0
# Exportar a Word (.docx real con el diseño reconstruido, ver exportword.py).
# Opcional: sin ella la app cae al antiguo HTML .doc.
python-docx>=1.1
# Detección de tablas en escaneos (scantables.py analiza la imagen de la página).
numpy>=1.26
# Exportar a Excel .xlsx real; sin ella la app cae al antiguo HTML .xls.
openpyxl>=3.1
